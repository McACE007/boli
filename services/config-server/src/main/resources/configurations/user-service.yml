spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/boli
    username: boli
    password: boli
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8090
